---
- name: Create folder tree structure
  hosts: localhost
  become: true
  tasks:
    - name: Create folders in the operating system
      ansible.builtin.file:
        path: "{{ item.path }}"
        state: directory
        mode: "{{ item.mode | default('0755') }}"
        owner: "{{ item.owner | default('root') }}"
        group: "{{ item.group | default('root') }}"
      loop:
        - path: /Users/{{ ansible_user }}/Documents/Projects
          owner: "{{ ansible_user }}"
          group: "{{ ansible_user }}"
        - path: /Users/{{ ansible_user }}/Documents/Rapports
          owner: "{{ ansible_user }}"
          group: "{{ ansible_user }}"
        - path: /Users/{{ ansible_user }}/Documents/RunJS
          owner: "{{ ansible_user }}"
          group: "{{ ansible_user }}"
        - path: "/Users/{{ ansible_user }}/Pictures/Screenshots"
          owner: "{{ ansible_user }}"
          group: "{{ ansible_user }}"
