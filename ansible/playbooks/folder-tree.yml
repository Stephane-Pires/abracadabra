---
- name: Create folder tree structure
  hosts: localhost
  become: true
  tasks:
    - name: Create folders in the operating system
      ansible.builtin.file:
        path: "{{ item.path }}"
        state: directory
        mode: "{{ item.mode | default('0755') }}"
        owner: "{{ item.owner | default('root') }}"
        group: "{{ item.group | default('root') }}"
      loop:
        - path: /Users/{{ ansible_user_id }}/Documents/Projects
          owner: "{{ ansible_user_id }}"
          group: "{{ ansible_user_id }}"
        - path: /Users/{{ ansible_user_id }}/Documents/Rapports
          owner: "{{ ansible_user_id }}"
          group: "{{ ansible_user_id }}"
        - path: /Users/{{ ansible_user_id }}/Documents/RunJS
          owner: "{{ ansible_user_id }}"
          group: "{{ ansible_user_id }}"
        - path: "/Users/{{ ansible_user_id }}/Pictures/Screenshots"
          owner: "{{ ansible_user_id }}"
          group: "{{ ansible_user_id }}"
